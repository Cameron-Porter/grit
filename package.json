{
  "name": "grit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix web run dev --",
    "web": "npm --prefix web run dev",
    "pwa:dev": "npm --prefix web run dev --",
    "pwa:build": "npm --prefix web run build",
    "pwa:test": "npm --prefix web test",
    "pwa:typecheck": "npm --prefix web run typecheck",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web test",
    "dev": "npm --prefix web run dev --",
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck",
    "test:rules": "npm --prefix web exec -- vitest run --config web/vitest.rules.config.mts",
    "pwa:e2e": "npm --prefix web run test:e2e",
    "test:unit": "npm --prefix web run test:unit",
    "audit": "npm --prefix web run audit",
    "quality": "npm --prefix web run quality"
  }
}
