import type{ExerciseProgress}from'@/lib/progress/metrics';
export function ProgressDashboard({records}:{records:ExerciseProgress[]}){if(!records.length)return <section className="surface empty-state"><h2>No personal records yet</h2><p>Complete weighted sets and your records will appear here.</p></section>;return <section><div className="section-heading"><h2>Current personal records</h2><span>{records.length} exercises</span></div><div className="pr-grid">{records.map(item=><article className="surface pr-card" key={item.exerciseName}><div><h3>{item.exerciseName}</h3><p>{new Date(item.achievedAt).toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'})}</p></div><strong>{item.bestWeight} lb × {item.bestReps}</strong></article>)}</div></section>}
